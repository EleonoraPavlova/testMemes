import { Card, CardBody } from "@heroui/react";
import { ReactElement } from "react";

const CardHeroui = (): ReactElement => {
  return (
    <Card>
      <CardBody>
        <p>Make beautiful websites regardless of your design experience.</p>
      </CardBody>
    </Card>
  );
};
export default CardHeroui;
